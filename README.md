# Installation

```bash
npm install -D jest-text-transformer
```

# Usage

In your `package.json` add these lines:

```json
"jest": {
    "transform": {
      "^.+\\.txt$": "jest-text-transformer"
    }
  }
```
